-- Trailhead — src/features/ShoppingList.lua
--
-- Aggregates required materials across remaining crafting steps.
-- Phase 1: stub.

local _, ns = ...

local ShoppingList = {}
ns.ShoppingList = ShoppingList

-- TODO(phase-4):
--   :Generate(guideKey, currentSkill) → { [itemName] = count }
--   :Render(parentFrame)
--   :ExportToClipboard()